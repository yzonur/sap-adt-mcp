node_modules/
config.json
*.log
.env

# Local digests / scratch outputs from scripts
*-digest.json

# Offline ownership proof — holds the secret root key. NEVER commit.
PROVENANCE.local.md

.DS_Store
